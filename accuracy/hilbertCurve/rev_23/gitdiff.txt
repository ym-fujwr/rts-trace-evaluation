diff --git a/src/main/java/org/davidmoten/hilbert/Ranges2.java b/src/main/java/org/davidmoten/hilbert/Ranges2.java
index 865d293..87d0fe3 100644
--- a/src/main/java/org/davidmoten/hilbert/Ranges2.java
+++ b/src/main/java/org/davidmoten/hilbert/Ranges2.java
@@ -15,2 +15,2 @@ public class Ranges2 implements Iterable<Range> {
-    private Node ranges;
-    private int count;
+    private Node ranges; // in descending order of ranges e.g. Range(5,7) -> Range(1,3)
+    private int count; // count of items in ranges
@@ -26 +26,2 @@ public class Ranges2 implements Iterable<Range> {
-        Preconditions.checkArgument(ranges == null || ranges.value.high() < r.low());
+        Preconditions.checkArgument(ranges == null || ranges.value.high() < r.low(),
+                "ranges must be added in increasing order and without overlap");
@@ -48,0 +50 @@ public class Ranges2 implements Iterable<Range> {
+
