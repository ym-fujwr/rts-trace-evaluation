diff --git a/src/main/java/org/apache/commons/cli/HelpFormatter.java b/src/main/java/org/apache/commons/cli/HelpFormatter.java
index 694ff036..81cc03c7 100644
--- a/src/main/java/org/apache/commons/cli/HelpFormatter.java
+++ b/src/main/java/org/apache/commons/cli/HelpFormatter.java
@@ -485 +485 @@ public class HelpFormatter {
-        if (header != null && !header.trim().isEmpty()) {
+        if (header != null && !header.isEmpty()) {
@@ -491 +491 @@ public class HelpFormatter {
-        if (footer != null && !footer.trim().isEmpty()) {
+        if (footer != null && !footer.isEmpty()) {
diff --git a/src/test/java/org/apache/commons/cli/HelpFormatterTest.java b/src/test/java/org/apache/commons/cli/HelpFormatterTest.java
index d391b8a4..30420349 100644
--- a/src/test/java/org/apache/commons/cli/HelpFormatterTest.java
+++ b/src/test/java/org/apache/commons/cli/HelpFormatterTest.java
@@ -295,0 +296,56 @@ public class HelpFormatterTest {
+    @Test
+    public void testPrintHelpNewlineFooter()
+    {
+        final HelpFormatter formatter = new HelpFormatter();
+        final ByteArrayOutputStream out = new ByteArrayOutputStream();
+        final PrintWriter pw = new PrintWriter(out);
+
+        final Options options = new Options();
+        options.addOption("a", "b");
+
+        formatter.printHelp(
+            pw,
+            80,
+            "test" + EOL,
+            "header" + EOL,
+            options,
+            0,
+            0,
+            EOL
+        );
+        final String expected = "usage: test" + EOL +
+                          "header" + EOL +
+                          "-ab" + EOL +
+                          EOL;
+        pw.flush();
+        assertEquals("footer newline", expected, out.toString());
+    }
+
+    @Test
+    public void testPrintHelpNewlineHeader()
+    {
+        final HelpFormatter formatter = new HelpFormatter();
+        final ByteArrayOutputStream out = new ByteArrayOutputStream();
+        final PrintWriter pw = new PrintWriter(out);
+
+        final Options options = new Options();
+        options.addOption("a", "b");
+
+        formatter.printHelp(
+            pw,
+            80,
+            "test" + EOL,
+            EOL,
+            options,
+            0,
+            0,
+            "footer" + EOL
+        );
+        String expected = "usage: test" + EOL +
+                          EOL +
+                          "-ab" + EOL +
+                          "footer" + EOL;
+        pw.flush();
+        assertEquals("header newline", expected, out.toString());
+    }
+
